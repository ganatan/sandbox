install_frontend_angular:
  stage: install
  image: node:20
  script:
    - cd "${PROJECT_DIR:-.}"
    - echo "📂 Checking files before npm ci"
    - ls -la
    - echo "✅ Node version:"
    - node -v
    - npm ci

# test_frontend_angular:
#   stage: test
#   image: node:20
#   script:
#     - cd "${PROJECT_DIR:-.}"
#     - echo "🧪 Running unit tests for Angular Starter Bootstrap"
#     - npm run test -- --watch=false --browsers=ChromeHeadless
#   dependencies:
#     - install_frontend_angular

# stages:
#   - install

# install:
#   stage: install
#   image: node:20
#   script:
#     - npm ci


# build:bootstrap:
#   stage: build
#   image: node:20
#   script:
#     - echo "Building Angular Starter Bootstrap project"
#     - npm run build
#   dependencies:
#     - install:bootstrap
#   artifacts:
#     paths:
#       - dist/
#     expire_in: 1 week
